version: '3.8'

services:
  app:
    build: .
    ports:
      - "3333:3333"
    environment:
      TZ: UTC
      PORT: 3333
      HOST: 0.0.0.0
      NODE_ENV: production
      LOG_LEVEL: info
      SESSION_DRIVER: cookie
      APP_KEY: ${APP_KEY}
      DB_CONNECTION: postgres
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_DATABASE: ${DB_DATABASE}
      SMTP_HOST: ${SMTP_HOST}           ← ADD THIS
      SMTP_PORT: ${SMTP_PORT}           ← ADD THIS
      SMTP_USERNAME: ${SMTP_USERNAME}   ← ADD THIS
      SMTP_PASSWORD: ${SMTP_PASSWORD}   ← ADD THIS
    depends_on:
      db:
        condition: service_healthy
    restart: on-failure

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_DATABASE}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data: